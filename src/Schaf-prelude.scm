(define import list)
(define scheme list)
(define base #f)
(define write #f)
(define time #f)

(define (flush-output-port . l)
  #f)

(define (jiffies-per-second)
  1)

(define current-second _cputime)

(define current-jiffy _cputime)
